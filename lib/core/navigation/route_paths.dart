/// Constant route path segments used by AppRouter.
///
/// All navigation paths are defined here as constants to avoid string
/// duplication and typos. No financial feature paths are defined in Phase 1.
abstract final class RoutePaths {
  /// Foundation smoke screen — the only route in Phase 1.
  static const String smoke = '/';
}
