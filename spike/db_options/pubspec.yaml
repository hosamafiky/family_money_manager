name: db_spike
description: "Disposable compilation spike for DECISION-004. Not production code."
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.12.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  drift_flutter: ^0.3.1
  drift: ^2.34.0

dev_dependencies:
  build_runner: ^2.15.1
  drift_dev: ^2.34.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
